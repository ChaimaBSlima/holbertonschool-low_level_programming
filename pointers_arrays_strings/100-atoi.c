#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _strcpy - a function that convert a string to an integer.
 * @s: the string
 * Return: int value 
 */
int _atoi(char *s)
{
	return(atoi(s));
}
