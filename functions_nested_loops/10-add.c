#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result.
 * @a:The first number a
 * @b:The second number b
 * Return: int value
 */
int add(int a, int b);
{
	return (a+b);
}
